function Wish({ name, msg="Good Night" }) {
  return (
    <h1>
    
      {msg} {name}
    </h1>
  );

  //   return <h1>{props.msg} {props.name} id{props.id}
  //   Objecty ={props.obj.id}
  //   </h1>;
}
export default Wish;
